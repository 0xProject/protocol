/**
 * Compute the bloom filter for a list of tokens.
 * Used to filter greedy tokens in the exchange proxy.
 */
export declare function getTokenListBloomFilter(tokens: string[]): string;
//# sourceMappingURL=bloom_filter_utils.d.ts.map