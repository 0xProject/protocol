{
    "name": "integrator-db",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "prisma generate",
        "db:migrate": "prisma migrate dev",
        "db:reset": "prisma migrate reset"
    },
    "devDependencies": {
        "jest": "^29.4.2",
        "prisma": "^4.8.1"
    },
    "dependencies": {
        "@prisma/client": "^4.10.1"
    }
}
