{
    "extends": ["//"],
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["__build__/**"]
        },
        "test:ci": {
            "dependsOn": ["build"],
            "outputs": ["__build__/**"]
        }
    }
}
